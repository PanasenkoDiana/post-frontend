// Импорт не используется, нужно убрать. емае ты что на 16 версии реакта пишешь, где еще импорты надо было делать
import React from 'react';
import './Main.css';
// props
export function Main() {
    return (
        <div className="main-container">
            {/* props для использования в Layout */}
        </div>
    );
}
